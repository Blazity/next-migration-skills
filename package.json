{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p skills/nextjs-migration-toolkit/tsconfig.json",
    "lint": "tsc -p skills/nextjs-migration-toolkit/tsconfig.json --noEmit",
    "test": "vitest",
    "test:run": "vitest run",
    "toolkit:install": "cd skills/nextjs-migration-toolkit && npm install"
  },
  "devDependencies": {
    "@types/node": "^25.2.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
